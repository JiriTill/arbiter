# Nixpacks configuration for Railway deployment
# Only adds system dependencies, doesn't override Python install

[phases.setup]
nixPkgs = ["tesseract", "poppler_utils", "python311", "python311Packages.pip"]
